# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Local Supabase keys (copy config.example.js -> config.js)
config.js

# Local dev server helper
serve.ps1

# Per-machine Claude Code settings (shared config lives in .claude/commands)
.claude/settings.local.json
